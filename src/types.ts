export interface Message {
  authorId: string
  id: string
  text: string
  timestamp: number
}

export interface Size {
  height: number
  width: number
}

export interface User {
  id: string
  name: string
}
