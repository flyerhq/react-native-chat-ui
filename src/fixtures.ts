import { Message, Size, User } from './types'

const message: Message = {
  authorId: 'userId',
  id: 'uuidv4',
  text: 'text',
  timestamp: 0,
}

const size: Size = {
  height: 0,
  width: 0,
}

const user: User = {
  id: 'userId',
  name: 'Alex',
}

export { message, size, user }
